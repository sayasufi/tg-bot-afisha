__pycache__/
.pytest_cache/
.ruff_cache/
*.pyc
*.pyo
*.pyd
.venv/
node_modules/
miniapp/node_modules/
miniapp/dist/
.env
.env.local
.tmp_*
*.session
screens/
celerybeat-schedule

# Personal browser cookie export — never commit
cookies.txt
# adstat scraper session cookies (Telemetr/TGStat) — server secret, never commit
secrets/
*cookies*.txt
# TG account packages (Telethon sessions / tdata / api-json) — secrets, never commit
+*/
tdata/
*.session-journal

# Playwright MCP browser scratch
.playwright-mcp/

# local audit scratch
audit_digest.txt

# dedup eval scratch
dedup_eval.py
titles_corpus.txt
infra/odyssey/odyssey.conf

# stray agent/tool-result artifacts (a workflow agent once wrote a mangled path here)
*tool-results*
*frontend_diff.txt
